//let sets the variable, myName names it, Salvija is the value of that variable
let myName = 'Salvija';
document.write(myName);
//by writting name of the variable here we give it a new value
myName = 'Jane Doe';
document.write(myName);

let simpleAddition = 2 + 2;
document.write(simpleAddition);

let size = 100;
let doubleSize = size * 2;
let minSize = (doubleSize * 2) - (size / 2);
document.write(minSize);
